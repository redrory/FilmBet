

				<% if current_user %>
				 	<div id ="welcome"><%= current_user.name %>, You have 10 points. </div>
				<% else %>
				
				<% redirect_to root_url %>	
				<% end %>

			<div id = "vote form">
				<%= simple_form_for @movie do |f| %>
					<ol>
						<li><%#= f.input :movie_one, :placeholder => @firstMovie, :label => false %> </li>
						<li><%= f.input :movie_one, :collection => @new_movie_list, :label => false , :value_method =>:title ,:include_blank => false %> </li>
						<li><%= f.input :movie_two, :collection => @new_movie_list, :label => false, :value_method =>:title ,:include_blank => false %> </li>
						<li><%= f.input :movie_three, :collection => @new_movie_list, :label => false, :value_method =>:title ,:include_blank => false %> </li>
						<li><%= f.input :movie_four,:collection => @new_movie_list, :label => false, :value_method =>:title ,:include_blank => false %> </li>

						<%= f.submit :value => " GO " %>
						</ol>		
		  		<%end %>	
		  		
		  </div>
		  		Last week results	
		  	<div id = "lastweek">
				  		<%# @employees.each do |vacay| %>
				  		<% @movie_list.each do |m| %>
				  		<ul>
				  			<li><%= m.title %></li>
				  		</ul>
			  		<% end %>
			 	</div>
			 </div>


		<% if current_user %>
		<span class="num"><%= link_to "Signout", signout_path %></span>
		<% end %>

	